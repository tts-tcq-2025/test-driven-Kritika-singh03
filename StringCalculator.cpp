#include "StringCalculator.h"

int StringCalculator::Add(const std::string& numbers) {
    if (numbers.empty()) return 0;  // Step 1: minimal code for first test
    return 0; // fallback, will evolve as we add tests
}
